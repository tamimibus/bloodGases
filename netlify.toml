[build]
  command = "npm run build"
  publish = "dist/public"  # Client assets for CDN
  
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
  force = true

[functions]
  node_bundler = "esbuild"
  external_node_modules = []  # Bundle everything
